<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MagdyStudent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/student_primary</item>
        <item name="colorPrimaryContainer">@color/student_primary_variant</item>
        <item name="colorSecondary">@color/student_secondary</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/student_primary</item>
        <item name="android:navigationBarColor">@color/student_primary</item>
        <item name="android:windowBackground">@color/student_background</item>
    </style>
</resources>

